{% extends 'base.html.twig' %}

{% block title %}Rating{% endblock %}

{% block body %}
    <h1>Rating</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rating.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ rating.value }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ rating.comment }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ rating.createdAt ? rating.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rating_api_index') }}">back to list</a>

    <a href="{{ path('app_rating_api_edit', {'id': rating.id}) }}">edit</a>

    {{ include('rating_api/_delete_form.html.twig') }}
{% endblock %}
