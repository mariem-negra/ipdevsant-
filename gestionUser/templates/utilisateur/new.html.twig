  <link href="{{ asset('imgBack/logo2.png')}}" rel="icon">

{% block body %}
    <style>
               /* New input group styling for icons */
        .input-wrapper {
            position: relative;
        }
            .icon-name { color: #4A90E2; }       /* Blue for name */
        .icon-email { color: #F5A623; }      /* Orange for email */
        .icon-password { color: #7ED321; }   /* Green for password */
        .icon-role { color: #BD10E0; }       /* Purple for role */
        .icon-date { color: #D0021B; }       /* Red for date */
        .icon-phone { color: #50E3C2; }      /* Turquoise for phone */
        .icon-specialty { color: #9013FE; }  /* Violet for specialty */
        .icon-diploma { color: #F5A623; }    /* Orange for diploma */

        body {
            background: linear-gradient(-45deg, #3fbbc0, #2a9d9f, #1c7a7d, #0d4d4f);
                animation: gradient 15s ease infinite;
        background-size: 400% 400%;

            font-family: 'Arial', sans-serif;
            margin: 0;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }
        @keyframes gradient {
            0% {
            background-position: 0% 50%;
            }
            50% {
            background-position: 100% 50%;
            }
            100% {
            background-position: 0% 50%;
            }
        }
        .form-container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
            padding: 2.5rem;
            width: 100%;
            max-width: 500px;
            text-align: center;
        }

        .login-logo {
            max-width: 250px;
            height: auto;
            margin-bottom: 1.5rem;
        }

        .page-title {
            color: #0066CC;
            font-size: 2rem;
            margin-bottom: 2rem;
            font-weight: 600;
        }

        .form-group {
            margin-bottom: 1.5rem;
            text-align: left;
        }

        .input-wrapper {
            position: relative;
        }
        .icon-name { color: #4A90E2; }
        .icon-email { color: #F5A623; }
        .icon-password { color: #7ED321; }
        .icon-role { color: #BD10E0; }
        .icon-date { color: #D0021B; }
        .icon-phone { color: #50E3C2; }
        .icon-specialty { color: #9013FE; }
        .icon-diploma { color: #F5A623; }

        

        .form-container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
            padding: 2.5rem;
            width: 100%;
            max-width: 500px;
            text-align: center;
        }

        /* New styles for the grid layout */
        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
            text-align: left;
        }

        /* In the grid, remove bottom margin from form-groups */
        .form-row .form-group {
            margin-bottom: 0;
        }

        .form-label {
            color: #0066CC;
            display: block;
            margin-bottom: 0.5rem;
            font-size: 1rem;
        }

        .input-wrapper {
            position: relative;
        }
        .icon-name { color: #4A90E2; }
        .icon-email { color: #F5A623; }
        .icon-password { color: #7ED321; }
        .icon-role { color: #BD10E0; }
        .icon-date { color: #D0021B; }
        .icon-phone { color: #50E3C2; }
        .icon-specialty { color: #9013FE; }
        .icon-diploma { color: #F5A623; }


        .form-container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
            padding: 2.5rem;
            width: 100%;
            max-width: 500px;
            text-align: center;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
            text-align: left;
        }

        .form-row .form-group {
            margin-bottom: 0;
        }

        /* New styles for label and icon */
        .label-wrapper {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 0.5rem;
        }

        .form-label {
            color: #0066CC;
            font-size: 1rem;
            margin: 0; /* Remove default margin */
        }

        .label-icon {
            font-size: 1rem;
        }

        /* Remove the previous icon positioning since we're moving them next to labels */
        .form-input {
            width: 100%;
            padding: 0.8rem;
            border: 1px solid #E5E5E5;
            border-radius: 8px;
            font-size: 1rem;
            outline: none;
            transition: border-color 0.3s ease;
            box-sizing: border-box;
        }

        .form-input:focus {
            border-color: #39A9A9;
        }

        .error-message {
            color: #FF4B4B;
            font-size: 0.85rem;
            margin-top: 0.5rem;
        }

        .submit-button {
            background-color: #3fbbc0;
            color: white;
            border: none;
            border-radius: 8px;
            padding: 1rem;
            width: 100%;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin-top: 1rem;
        }

        .submit-button:hover {
            background-color: #2E8B8B;
        }

        .checkbox-group {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin: 1rem 0;
        }

        .checkbox-group input[type="checkbox"] {
            width: 16px;
            height: 16px;
            border: 1px solid #E5E5E5;
            border-radius: 4px;
        }

        @media (max-width: 768px) {
            .form-container {
                margin: 1rem;
                padding: 1.5rem;
            }

            .login-logo {
                max-width: 200px;
            }
        }
        /* Existing styles remain the same until form-group */


        /* Rest of your existing styles... */

        @media (max-width: 768px) {
            .form-row {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
        }
    </style>

    <div class="form-container">
        <img src="{{ asset('img/logo.png') }}" alt="Chronoserena" class="login-logo">
        
        <h1 class="page-title">Créez votre compte</h1>

        {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class': 'registration-form', 'enctype': 'multipart/form-data'}}) }}

            <!-- Nom and Prenom side by side -->
            <div class="form-row">
                <div class="form-group">
                    <div class="label-wrapper">
                        <i class="bi bi-person-fill label-icon icon-name"></i>
                        <label class="form-label" for="{{ form.nom.vars.id }}">Nom</label>
                    </div>
                    {{ form_widget(form.nom, {'attr': {'class': 'form-input', 'placeholder': 'Entrez votre nom'}}) }}
                    <div class="error-message">{{ form_errors(form.nom) }}</div>
                </div>

                <div class="form-group">
                    <div class="label-wrapper">
                        <i class="bi bi-person-fill label-icon icon-name"></i>
                        <label class="form-label" for="{{ form.prenom.vars.id }}">Prénom</label>
                    </div>
                    {{ form_widget(form.prenom, {'attr': {'class': 'form-input', 'placeholder': 'Entrez votre prénom'}}) }}
                    <div class="error-message">{{ form_errors(form.prenom) }}</div>
                </div>
            </div>

            <div class="form-group">
                <div class="label-wrapper">
                    <i class="bi bi-envelope-fill label-icon icon-email"></i>
                    <label class="form-label" for="{{ form.email.vars.id }}">Email</label>
                </div>
                {{ form_widget(form.email, {'attr': {'class': 'form-input', 'placeholder': 'Entrez votre email'}}) }}
                <div class="error-message">{{ form_errors(form.email) }}</div>
            </div>

            <div class="form-group">
                <div class="label-wrapper">
                    <i class="bi bi-lock-fill label-icon icon-password"></i>
                    <label class="form-label" for="{{ form.motDePasse.vars.id }}">Mot de passe</label>
                </div>
                {{ form_widget(form.motDePasse, {'attr': {'class': 'form-input', 'placeholder': 'Entrez votre mot de passe'}}) }}
                <div class="error-message">{{ form_errors(form.motDePasse) }}</div>
            </div>

            <div class="form-group">
                <div class="label-wrapper">
                    <i class="bi bi-person-badge-fill label-icon icon-role"></i>
                    <label class="form-label" for="{{ form.role.vars.id }}">Rôle</label>
                </div>
                {{ form_widget(form.role, {'attr': {'class': 'form-input'}}) }}
                <div class="error-message">{{ form_errors(form.role) }}</div>
            </div>

            <!-- Telephone and Date de naissance side by side -->
            <div class="form-row">
                <div class="form-group">
                    <div class="label-wrapper">
                        <i class="bi bi-telephone-fill label-icon icon-phone"></i>
                        <label class="form-label" for="{{ form.telephone.vars.id }}">Téléphone</label>
                    </div>
                    {{ form_widget(form.telephone, {'attr': {'class': 'form-input', 'placeholder': 'Entrez votre numéro de téléphone'}}) }}
                    <div class="error-message">{{ form_errors(form.telephone) }}</div>
                </div>

                <div class="form-group">
                    <div class="label-wrapper">
                        <i class="bi bi-calendar-fill label-icon icon-date"></i>
                        <label class="form-label" for="{{ form.dateNaissance.vars.id }}">Date de naissance</label>
                    </div>
                    {{ form_widget(form.dateNaissance, {'attr': {'class': 'form-input'}}) }}
                    <div class="error-message">{{ form_errors(form.dateNaissance) }}</div>
                </div>
            </div>

            {% if form.specialite is defined %}
                <div class="form-group">
                    <div class="label-wrapper">
                        <i class="bi bi-heart-pulse-fill label-icon icon-specialty"></i>
                        <label class="form-label" for="{{ form.specialite.vars.id }}">Spécialité</label>
                    </div>
                    {{ form_widget(form.specialite, {'attr': {'class': 'form-input'}}) }}
                    <div class="error-message">{{ form_errors(form.specialite) }}</div>
                </div>
            {% endif %}

            {% if form.diploma is defined %}
                <div class="form-group">
                    <div class="label-wrapper">
                        <i class="bi bi-mortarboard-fill label-icon icon-diploma"></i>
                        <label class="form-label" for="{{ form.diploma.vars.id }}">Diplôme</label>
                    </div>
                    {{ form_widget(form.diploma, {'attr': {'class': 'form-input'}}) }}
                    <div class="error-message">{{ form_errors(form.diploma) }}</div>
                </div>
            {% endif %}

            <button type="submit" class="submit-button">
                Créer mon compte
            </button>

        {{ form_end(form) }}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    </div>
<script>
    async function registerWebAuthn() {
    // Get options
    const optionsResponse = await fetch('/webauthn/register/options', {
        method: 'POST',
        credentials: 'same-origin'
    });
    const options = await optionsResponse.json();
    
    // Create credentials
    const credential = await navigator.credentials.create({
        publicKey: options
    });
    
    // Verify registration
    const verification = await fetch('/webauthn/register/verify', {
        method: 'POST',
        credentials: 'same-origin',
        body: JSON.stringify(credential)
    });
    
    return verification.json();
}
</script>

{% endblock %}